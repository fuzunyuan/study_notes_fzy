# 设计步骤
## 核心机制
1. 当我们复制一个shared_ptr的时候，不能像unique_ptr那样转移所有权，我们必须知道“总共有多少个shared_ptr”在指向这个资源
2. 解决方案：控制块
   a. 我们需要在堆上额外分配一个小对象，这个对象我们称之为控制块
   b. 所有共享同一个资源的shared_ptr的实例，都将共享指向同一个控制块
3. 控制块的内容
   a. string_cout（强引用计数）: 记录有多少个shared_ptr正在拥有这个资源
## 实现细节